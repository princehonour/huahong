<view class="container">
  <view class="content" hidden="{{JSON.stringify(healthInfo) === '{}'}}">
    <scroll-view scroll-y="true" class="scroll">
    
    </scroll-view>
  </view>
  <view class="no-content" hidden="{{!(JSON.stringify(healthInfo) === '{}')}}">
    暂无信息
  </view>
</view>